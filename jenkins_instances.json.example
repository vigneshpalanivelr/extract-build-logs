{
  "instances": [
    {
      "jenkins_url": "https://jenkins1.example.com",
      "jenkins_user": "jenkins",
      "jenkins_api_token": "jenkins_user_token",
      "token_encoding": "plain",
      "jenkins_webhook_secret": "webhook_secret_token_1",
      "secret_encoding": "plain",
      "description": "Old Jenkins instance"
    },
    {
      "jenkins_url": "https://jenkins2.example.com",
      "jenkins_user": "jenkins",
      "jenkins_api_token": "jenkins_user_token",
      "token_encoding": "base64",
      "jenkins_webhook_secret": "webhook_secret_token_2==",
      "secret_encoding": "base64",
      "description": "DAS Jenkins instance"
    },
    {
      "jenkins_url": "https://jenkins3.example.com",
      "jenkins_user": "jenkins",
      "jenkins_api_token": "jenkins_user_token",
      "description": "Jenkins instance (no webhook secret, plain text token)"
    }
  ],
  "_comment": "Note: token_encoding and secret_encoding are optional and default to 'plain'. Set to 'base64' to decode credentials. Use: echo -n 'your_token' | base64 to encode."
}
